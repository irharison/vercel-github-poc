# Archive

Backup of Labour–Fabian public directory site archived Sep 2026 before main was cleared for reuse.

This branch (`archive/fabian-directory`) is a full snapshot of the unofficial Fabian Society public directory as it existed on `main` at commit `f57d32bf8848ad95501f6851241f3bced67fdb93` (12 Sep 2026).

The live site was https://vercel-github-poc.vercel.app. After this archive, `main` is a clean placeholder so the repo can be reused. Nothing here was deleted from git history.

To browse this snapshot:

```bash
git checkout archive/fabian-directory
```

Tag: `archive/fabian-directory-2026-09`
